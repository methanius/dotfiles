# Placeholder for Wayland desktop environment (sway, hyprland, or niri).
# This module should be imported only on hosts with a graphical session.
{...}: {
  # wayland.windowManager.sway.enable = true;
  # wayland.windowManager.hyprland.enable = true;
}
