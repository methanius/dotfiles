# Placeholder NixOS system configuration.
# This will be populated when merging with the NixOS machine config.
{
  pkgs,
  lib,
  ...
}: {
  # system.stateVersion = "25.05";
  # networking.hostName = "nixos";

  # Home Manager as a NixOS module is configured in flake.nix.
  # Host-specific system packages, services, boot config, hardware, etc. go here.
}
